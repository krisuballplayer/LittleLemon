import Logo from "./assets/Logo.png";

const Header = () => {
  return (
    <header>
      <h1>HEADER</h1>
      <img src={Logo} alt="Logo" />
      HEADER
    </header>
  );
};

export default Header;
